{
  "name": "deployments",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.jsx --minify --bundle --outdir=public/build --loader:.js=jsx",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.25.1"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
